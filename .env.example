# iNomad uses MapLibre + OpenFreeMap (OSS, no API key required).
# This file is kept for future env vars but currently has no required entries.
#
# Optional: override the MapLibre style URL in screen/MapScreen.js if you want
# different tiles. Any MapLibre-compatible vector style works.
